# 汉语 Flashcards — HSK 3.0

Web app ทายคำศัพท์จีน: โชว์ตัวจีน → เลือกคำแปล (อังกฤษ + พินอิน) จาก 3 ตัวเลือก

คำศัพท์ 10,050 คำ จาก [drkameleon/complete-hsk-vocabulary](https://github.com/drkameleon/complete-hsk-vocabulary)
ใช้ list `wordlists/exclusive/newest` = **HSK 3.0 (มาตรฐานปี 2021)** ระดับ 1–6 และ 7–9

## รัน

```bash
cd public && python3 -m http.server 8000
# เปิด http://localhost:8000
```

ต้องเปิดผ่าน web server (เปิดไฟล์ตรงๆ ด้วย `file://` ไม่ได้ เพราะ `fetch()` โดน CORS บล็อก)
เป็น static site ล้วน — เอาไป deploy บน GitHub Pages / Netlify / Vercel ได้เลยโดยไม่ต้องแก้อะไร

## ฟีเจอร์

| | |
|---|---|
| ระดับ | เลือกได้หลายระดับพร้อมกัน (1 / 2 / 3 / 4 / 5 / 6 / 7-9) |
| โหมด | 汉字 → คำแปล+พินอิน · คำแปล → 汉字 · 汉字 → พินอิน |
| จำนวนข้อ | 10 / 20 / 50 / ทั้งหมด |
| เรียงลำดับ | สุ่ม หรือ ตามความถี่การใช้งาน (ง่าย→ยาก) |
| เสียง | `speechSynthesis` ภาษา zh-CN — กดปุ่ม 🔊 หรือเปิดอ่านอัตโนมัติ |
| สรุปผล | คะแนน %, สตรีคสูงสุด, รายการคำที่ตอบผิด, ปุ่มทบทวนเฉพาะคำที่ผิด |
| คีย์ลัด | `1` `2` `3` ตอบ · `Enter`/`Space` ข้อต่อไป · `S` ออกเสียง · `Esc` ออก |

ตั้งค่าและสถิติเก็บใน `localStorage` (key `hsk-flash-v1`)

## โครงสร้าง

```
build/build_data.py     สคริปต์ดึง + แปลงข้อมูลจาก GitHub (ต้องมี python3 อย่างเดียว)
build/.cache/           ไฟล์ต้นฉบับที่โหลดมา (gitignored)
public/index.html
public/css/style.css
public/js/app.js
public/data/index.json  รายการระดับ + จำนวนคำ
public/data/hsk-N.json  [{s, t, py, en[], pos[], q}]  ~1.1 MB รวมทุกระดับ
```

`s` = ตัวย่อ · `t` = ตัวเต็ม (ว่าง ถ้าเหมือนตัวย่อ) · `py` = พินอิน · `en` = คำแปล ≤3 ความหมาย
`pos` = ชนิดคำ · `q` = อันดับความถี่ (0 = ไม่มีข้อมูล)

## สร้างข้อมูลใหม่

```bash
cd build && python3 build_data.py
```

โหลดจาก GitHub ครั้งแรกแล้ว cache ไว้ ลบ `build/.cache/` ถ้าอยากดึงข้อมูลล่าสุด

### หมายเหตุเรื่องการเลือกความหมาย

ต้นทางเก็บทุกเสียงอ่านของคำ และ **ไม่ได้เรียงตามความถี่** เช่น 听 ขึ้น `yǐn "smile (archaic)"`
มาก่อน `tīng` สคริปต์เลยให้คะแนนแต่ละเสียงอ่านแล้วเลือกตัวที่ดีที่สุด:

- ตัดความหมายที่ใช้เป็นคำตอบไม่ได้ (`surname X`, `variant of`, `abbr. for`, `CL:`)
- ลดคะแนนเสียงอ่านที่มีแต่ความหมายโบราณ `(archaic)/(old)/(literary)`, ที่เป็นคำเฉพาะ (พินอินขึ้นต้นตัวใหญ่ เช่น 日 `Rì`), และที่เป็นแค่ variant ของตัวอื่น
- เอาความหมายที่ใช้ทั่วไปขึ้นก่อนความหมายโบราณ

เหลือ 多音字 อีกกลุ่มที่ heuristic ตัดสินไม่ได้ (长 cháng/zhǎng, 重 zhòng/chóng, 空 kōng/kòng ฯลฯ)
ใช้ตาราง `OVERRIDES` ใน `build_data.py` กำหนดตรงๆ — เพิ่มได้ถ้าเจอคำที่เลือกเสียงอ่านผิด

### การเลือกตัวลวง

ตัวลวงสุ่มจากระดับเดียวกัน (ถ้าระดับนั้นมี ≥40 คำ) และตัดออกถ้า
คำแปลแรกซ้ำกัน หรือมีคำภาษาอังกฤษยาว ≥4 ตัวอักษรซ้ำกับตัวเลือกอื่น
ทดสอบครบทั้ง 10,050 คำแล้ว ไม่มีข้อไหนได้ตัวเลือกซ้ำหรือไม่ครบ 3 ตัว
